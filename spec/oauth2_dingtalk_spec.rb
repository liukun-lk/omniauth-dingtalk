require "spec_helper"

RSpec.describe Oauth2Dingtalk do
  it "has a version number" do
    expect(Oauth2Dingtalk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
